const bigPow = (n, pow) => {
	let p = n.toString();
	for ( ;  ; ) {
		
	}
	
};


(function main() {
	let power = bigPow(2, 1000);
	// power = power.toString();
	console.log(power);
	// console.log(power.reduce((acc, n) => acc.add(new BigNumber(n)), new BigNumber(0)));
})();

